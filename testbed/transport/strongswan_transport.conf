# StrongSwan Transport Mode Configuration
# PRD Case 3: Host-to-Host Transport Mode + AES-GCM + PFS ON

conn transport-mode
    keyexchange=ikev2
    ike=aes256gcm16-prfsha256-ecp256!
    esp=aes256gcm16-ecp256!
    left=10.0.0.5
    right=10.0.0.12
    type=transport
    auto=start
